-- Remove accrued_days column from balance table if it exists (no-op if not present)
-- If you want to add it back, uncomment the next line
-- ALTER TABLE balance ADD accrued_days INT NULL;

-- No action needed if you do not want accrued_days. If you want to add it, use the line above.
